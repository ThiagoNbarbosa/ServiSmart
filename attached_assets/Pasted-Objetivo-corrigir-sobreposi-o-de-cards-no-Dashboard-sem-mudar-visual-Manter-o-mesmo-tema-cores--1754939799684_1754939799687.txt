Objetivo: corrigir sobreposição de cards no Dashboard sem mudar visual. 
Manter o mesmo tema/cores/tipografia; apenas ajustar grid/altura/z-index/overflow.

1) Localizar arquivos do Dashboard:
- Procurar por componentes e textos: "Dashboard", "Distribuição por Status", "Tendência Mensal", "Performance por Técnico", "Atividade Recente".
- Candidatos: src/pages/Dashboard.tsx|tsx, src/pages/dashboard/index.tsx, src/components/dashboard/*, src/sections/dashboard/*.

2) Ajustar o grid principal dos cards médios:
- Garantir wrapper com: 
  className="grid grid-cols-1 lg:grid-cols-12 gap-4 lg:gap-6 auto-rows-auto"
- Distribuição por Status -> className="lg:col-span-6"
- Tendência Mensal -> className="lg:col-span-6"
- Performance por Técnico -> className="lg:col-span-7"
- Atividade Recente -> className="lg:col-span-5"

3) Remover posicionamentos absolutos desnecessários nos cards:
- Em cada Card/Chart, garantir `className="relative overflow-hidden"` no <Card>.
- Remover `position:absolute`, `-mt-*` e `z-10/z-20` de wrappers internos que causem flutuação.
- Onde houver `overflow-visible` no Card, trocar por `overflow-hidden` (manter sombra visível via wrapper externo com `relative`).

4) Altura explícita dos gráficos (evitar colapso do container):
- Para Recharts/vis lib semelhante, envolver o gráfico em:
  <div className="chart-wrap h-[280px] md:h-[300px]">
    <ResponsiveContainer width="100%" height="100%">...</ResponsiveContainer>
  </div>
- Se estiver usando height "auto", substituir por altura fixa no container (280–320px).
- Remover margens negativas no gráfico.

5) Z-index e isolamento:
- No container da seção do Dashboard, aplicar `className="relative isolate"`.
- Definir todos os Cards sem z-index personalizados (usar `z-0`); manter apenas o header/topbar com `z-20` se for sticky.

6) Espaçamentos verticais:
- Adicionar `gap-y-6` entre seções empilhadas.
- Garantir margem inferior dos cards superiores: `mb-4 md:mb-6` no bloco que contém os dois cards (“Distribuição” e “Tendência”).

7) Verificações:
- Testar em 1280x800, 1440x900 e 1024x768.
- Confirmar que “Tendência Mensal” não sobrepõe “Atividade Recente”.
- Confirmar que “Distribuição por Status” não invade “Performance por Técnico”.
- Não alterar textos, ícones, paleta e bordas; apenas layout.

8) Commits:
- Mensagem: "fix(dashboard): prevent card overlap by grid/height/z-index adjustments"
- Executar lint e build. 
